---
phase: 01-code-style-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Home/Home2.js
  - src/index.css
autonomous: true
requirements:
  - CLEAN-01
  - STYLE-01

must_haves:
  truths:
    - "Home2.js does not exist in the project"
    - "App.js contains no import or reference to Home2"
    - "Browser loads Lato and Nunito from Google Fonts CDN"
    - "Body text renders in Lato, not PT Mono"
  artifacts:
    - path: "src/index.css"
      provides: "Google Fonts @import for Lato + Nunito; no PT Mono body override"
      contains: "fonts.googleapis.com/css2?family=Lato"
  key_links:
    - from: "src/index.css"
      to: "Google Fonts CDN"
      via: "@import url at line 1"
      pattern: "@import url.*Lato.*Nunito"
    - from: "src/style.css"
      to: "browser body rendering"
      via: "body { font-family: \"Lato\", sans-serif } (already in style.css line 3)"
      pattern: "font-family.*Lato"
---

<objective>
Delete the orphaned Home2.js dead-code file and fix the font loading system so Lato (body) and Nunito (headings) actually resolve in the browser.

Purpose: CLEAN-01 removes a file that renders "WEBSITE UNDER DEVELOPMENT" and has no route — it's unreachable dead code. STYLE-01 fixes a font system broken since the original setup: style.css declares Lato for body and Nunito for headings, but index.css imports different fonts (Outfit, PT Mono) and overrides body with PT Mono !important, so neither Lato nor Nunito ever loads.

Output:
- Home2.js deleted (per D-03)
- src/index.css updated: @import swapped to Lato + Nunito, PT Mono body font-family removed (per D-01, D-02)
</objective>

<execution_context>
@C:/Users/Keigan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Keigan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-code-style-fixes/01-CONTEXT.md
@.planning/phases/01-code-style-fixes/01-RESEARCH.md
@.planning/phases/01-code-style-fixes/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Home2.js dead code (CLEAN-01, D-03)</name>
  <files>src/components/Home/Home2.js</files>
  <read_first>
    - src/components/Home/Home2.js — read to confirm it is the "WEBSITE UNDER DEVELOPMENT" stub with no router registration
    - src/App.js — confirm zero imports or JSX references to Home2 before deleting
  </read_first>
  <action>
    Delete the file src/components/Home/Home2.js. This is the only action required per D-03. App.js has been confirmed to have zero references to Home2 — no edits to App.js are needed.

    Use the Bash tool to delete: `rm /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/components/Home/Home2.js`

    Do NOT edit App.js, do NOT edit any other file, do NOT create a replacement.
  </action>
  <verify>
    <automated>ls /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/components/Home/Home2.js 2>&1 | grep -c "No such file" || echo "FAIL: file still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Home/Home2.js` does not exist on the filesystem (ls returns non-zero exit code / "No such file")
    - `src/App.js` is unchanged (no diff)
    - `grep -r "Home2" src/` returns zero matches
  </acceptance_criteria>
  <done>Home2.js deleted; no remaining references anywhere in src/</done>
</task>

<task type="auto">
  <name>Task 2: Fix font imports in index.css (STYLE-01, D-01, D-02)</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css — read the full file (it is short, ~9 lines) to see exact current content before editing
    - .planning/phases/01-code-style-fixes/01-PATTERNS.md — read the "src/index.css" section for the exact before/after target state
  </read_first>
  <action>
    Edit src/index.css to make exactly two changes:

    1. Replace line 1 — the @import for Outfit+PT Mono — with:
       @import url("https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&family=Nunito:wght@300;400;600;700&display=swap");

    2. Delete the line containing `font-family: "PT Mono", monospace !important;` from the body rule.

    All other lines in the file remain UNCHANGED. The @import must remain line 1 of the file (CSS spec: @import before any other rule).

    After the edit, the body rule in index.css contains only: margin, -webkit-font-smoothing, -moz-osx-font-smoothing, and background-image — no font-family declaration.

    This allows src/style.css line 3 (`body { font-family: "Lato", sans-serif; }`) to take effect, and the new @import supplies the Lato + Nunito font files from Google Fonts CDN.

    Do NOT touch src/style.css in this task. Do NOT add font-family to the body rule. Do NOT add any CSS rules before the @import.
  </action>
  <verify>
    <automated>grep -n "Lato" /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/index.css</automated>
    <automated>grep -c "PT Mono" /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/index.css</automated>
    <automated>grep -c "Outfit" /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Lato" src/index.css` returns a line 1 match containing `fonts.googleapis.com/css2?family=Lato`
    - `grep -c "PT Mono" src/index.css` returns 0
    - `grep -c "Outfit" src/index.css` returns 0
    - `grep -c "Nunito" src/index.css` returns 1 (in the @import URL on line 1)
    - `grep -n "@import" src/index.css` shows @import on line 1 only
    - The body rule in index.css contains no font-family declaration
  </acceptance_criteria>
  <done>index.css imports Lato+Nunito on line 1; PT Mono and Outfit removed; body font-family override deleted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.css → Google Fonts CDN | Browser makes outbound request to fonts.googleapis.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Google Fonts CDN @import | accept | CDN is controlled by Google; standard industry practice for font delivery; no PII transmitted; URL is hardcoded in source |
| T-01-02 | Denial of Service | Google Fonts CDN | accept | Font fallback (`sans-serif`) is declared in style.css body rule; UI degrades gracefully if CDN unavailable |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — file edits only |
</threat_model>

<verification>
After both tasks complete:

1. `ls src/components/Home/Home2.js` — must fail (file deleted)
2. `grep -r "Home2" src/` — must return zero results
3. `grep -n "@import" src/index.css` — must show Lato+Nunito on line 1
4. `grep -c "PT Mono" src/index.css` — must return 0
5. `grep -c "Outfit" src/index.css` — must return 0
6. `npm start` — dev server starts without errors
7. Browser DevTools Network tab (filter "fonts.gstatic.com") — Lato and Nunito font files appear as loaded network requests
8. Body text visually renders in Lato (sans-serif), not monospace
</verification>

<success_criteria>
- Home2.js does not exist and has zero references in the codebase
- src/index.css line 1 is `@import url("https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&family=Nunito:wght@300;400;600;700&display=swap");`
- src/index.css body rule contains no font-family declaration
- Browser loads Lato and Nunito from Google Fonts on page load
</success_criteria>

<output>
Create `.planning/phases/01-code-style-fixes/01-01-SUMMARY.md` when done.
</output>
