---
phase: 01-code-style-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/style.css
  - src/components/About/Techstack.js
  - src/Assets/TechIcons/numpy.svg
  - src/Assets/TechIcons/graph.svg
  - src/Assets/TechIcons/Matlab.svg
  - src/Assets/TechIcons/typescript.svg
  - src/Assets/TechIcons/n8n.svg
  - src/Assets/TechIcons/sentry.svg
autonomous: true
requirements:
  - CLEAN-02
  - STYLE-02

must_haves:
  truths:
    - "style.css contains exactly one standalone .navbar-nav .nav-link block"
    - "The merged .navbar-nav .nav-link block contains all properties from both original blocks"
    - "The media-query override at max-width 767px is untouched"
    - "All 16 img tags in Techstack.js have className=\"tech-icon-images\""
    - "Techstack icons render at 1.6rem height, matching Toolstack icons"
    - "All 6 new SVG files are tracked in git"
  artifacts:
    - path: "src/style.css"
      provides: "Single merged .navbar-nav .nav-link block with all properties"
      contains: "color: white !important"
    - path: "src/components/About/Techstack.js"
      provides: "All img tags with className=\"tech-icon-images\""
      min_lines: 90
  key_links:
    - from: "src/components/About/Techstack.js"
      to: "src/style.css .tech-icon-images rule"
      via: "className=\"tech-icon-images\" on every <img>"
      pattern: "className=\"tech-icon-images\""
    - from: "src/style.css .navbar-nav .nav-link (merged)"
      to: "browser nav rendering"
      via: "single canonical CSS block"
      pattern: "^\\.navbar-nav .nav-link \\{"
---

<objective>
Merge the duplicate .navbar-nav .nav-link CSS blocks in style.css into one canonical block, add className="tech-icon-images" to all 16 img tags in Techstack.js, and stage the 6 untracked SVG icon files for commit.

Purpose: CLEAN-02 eliminates a CSS redundancy where two blocks declare overlapping properties for the same selector — a maintenance hazard. STYLE-02 fixes the visual inconsistency where Techstack icons have no height constraint while Toolstack icons use the .tech-icon-images class (1.6rem). D-06 ensures the already-present SVG files are tracked in git as part of this phase's icon work.

Output:
- src/style.css: single merged .navbar-nav .nav-link block (per D-04)
- src/components/About/Techstack.js: all 16 img tags have className="tech-icon-images" (per D-05)
- 6 SVG files staged and committed: numpy.svg, graph.svg, Matlab.svg, typescript.svg, n8n.svg, sentry.svg (per D-06)
</objective>

<execution_context>
@C:/Users/Keigan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Keigan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-code-style-fixes/01-CONTEXT.md
@.planning/phases/01-code-style-fixes/01-RESEARCH.md
@.planning/phases/01-code-style-fixes/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge duplicate .navbar-nav .nav-link blocks in style.css (CLEAN-02, D-04)</name>
  <files>src/style.css</files>
  <read_first>
    - src/style.css — read lines 195–290 to see the exact current content of both duplicate blocks (lines 202–206 and 268–274) and the media-query block (lines 283–287) before editing
    - .planning/phases/01-code-style-fixes/01-PATTERNS.md — read the "src/style.css" section for the exact merged target block and property ordering rules
  </read_first>
  <action>
    Make two edits to src/style.css:

    EDIT 1 — Delete the block at lines 202–206 (the first standalone .navbar-nav .nav-link block). Remove all 5 lines:
      .navbar-nav .nav-link {
        color: white !important;
        padding-right: 1rem !important;
        padding-left: 1rem !important;
      }

    EDIT 2 — Replace the block at lines 268–274 (the second standalone .navbar-nav .nav-link block) with this merged block containing all properties from both deleted/replaced blocks:
      .navbar-nav .nav-link {
        color: white !important;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 0.8rem 1rem;
        padding-right: 1rem !important;
        padding-left: 1rem !important;
        line-height: 1;
      }

    CRITICAL property order: `padding` shorthand (line 6) MUST appear BEFORE `padding-right` and `padding-left` longhands (lines 7-8). If padding shorthand appears after the !important longhands, it overrides them and breaks nav padding. The !important longhands must win.

    Do NOT touch the @media (max-width: 767px) block that contains a .navbar-nav .nav-link override — that block is a responsive override, not a duplicate.

    Do NOT edit any other properties or blocks in style.css.
  </action>
  <verify>
    <automated>grep -c "^\.navbar-nav \.nav-link {" /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/style.css</automated>
    <automated>grep -n "^\.navbar-nav \.nav-link {" /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.navbar-nav \.nav-link {" src/style.css` returns 1 (exactly one standalone block; the media-query nested block uses indented selector so does not match `^`)
    - The single remaining block contains all 8 properties: color, display, justify-content, align-items, padding, padding-right, padding-left, line-height
    - `padding: 0.8rem 1rem;` appears on a line BEFORE `padding-right: 1rem !important;` within that block
    - The @media (max-width: 767px) block containing justify-content: flex-start and padding: 0.7rem 1rem is still present and unchanged
  </acceptance_criteria>
  <done>style.css has exactly one standalone .navbar-nav .nav-link block with all properties merged; media-query override untouched</done>
</task>

<task type="auto">
  <name>Task 2: Add className="tech-icon-images" to all Techstack.js img tags (STYLE-02, D-05) and stage new SVG files (D-06)</name>
  <files>src/components/About/Techstack.js, src/Assets/TechIcons/numpy.svg, src/Assets/TechIcons/graph.svg, src/Assets/TechIcons/Matlab.svg, src/Assets/TechIcons/typescript.svg, src/Assets/TechIcons/n8n.svg, src/Assets/TechIcons/sentry.svg</files>
  <read_first>
    - src/components/About/Techstack.js — read the full file to see all 16 img tags and their current attributes before editing
    - src/components/About/Toolstack.js — read lines 18–65 to see the exact reference pattern: every img has className="tech-icon-images" as the third attribute after src and alt
    - .planning/phases/01-code-style-fixes/01-PATTERNS.md — read the "src/components/About/Techstack.js" section for the complete list of all 16 img tags to update
  </read_first>
  <action>
    PART A — Edit src/components/About/Techstack.js:

    Add `className="tech-icon-images"` to every `<img>` tag in the file. There are exactly 16 img tags. The icons are: Python, numpy, Matplotlib, Pandas, C++, Julia, terminal, git, latex, javascript, Typescript, MATLAB, node, react, CSS, sql.

    Each img tag currently looks like: `<img src={IconVar} alt="IconName" />`
    Each img tag must become: `<img src={IconVar} alt="IconName" className="tech-icon-images" />`

    Place className as the third attribute, after src and alt, matching the Toolstack.js reference pattern exactly.

    After editing, verify the count with grep: `grep -c 'className="tech-icon-images"' src/components/About/Techstack.js` must return 16.

    PART B — Stage the 6 untracked SVG files for git commit:

    Stage all 6 untracked SVG files using git add:
    - src/Assets/TechIcons/numpy.svg
    - src/Assets/TechIcons/graph.svg
    - src/Assets/TechIcons/Matlab.svg
    - src/Assets/TechIcons/typescript.svg
    - src/Assets/TechIcons/n8n.svg
    - src/Assets/TechIcons/sentry.svg

    Use: `git add src/Assets/TechIcons/numpy.svg src/Assets/TechIcons/graph.svg src/Assets/TechIcons/Matlab.svg src/Assets/TechIcons/typescript.svg src/Assets/TechIcons/n8n.svg src/Assets/TechIcons/sentry.svg`

    Do NOT stage any other files at this step — the plan commit step will handle all staged files together.
  </action>
  <verify>
    <automated>grep -c 'className="tech-icon-images"' /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/components/About/Techstack.js</automated>
    <automated>grep -c '<img' /c/Users/Keigan/Desktop/PersonalWebsite/personal-website/src/components/About/Techstack.js</automated>
    <automated>git -C /c/Users/Keigan/Desktop/PersonalWebsite/personal-website diff --cached --name-only | grep "TechIcons"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'className="tech-icon-images"' src/components/About/Techstack.js` returns 16
    - `grep -c '<img' src/components/About/Techstack.js` returns 16 (same count — every img has the class)
    - No img tag in Techstack.js is missing className="tech-icon-images" (grep for `<img src=` without `className` returns 0)
    - `git diff --cached --name-only` includes all 6 SVG files: numpy.svg, graph.svg, Matlab.svg, typescript.svg, n8n.svg, sentry.svg
    - Techstack.js is modified (git diff --name-only shows it changed)
  </acceptance_criteria>
  <done>All 16 Techstack.js img tags have className="tech-icon-images"; all 6 new SVG files staged for commit</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG files → browser renderer | SVG files are static assets rendered by browser; no user-supplied content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SVG icon files in src/Assets/TechIcons/ | accept | Files are static assets committed to source control; no dynamic content or user input; rendered as img src (not innerHTML), so SVG script injection is not possible via this vector |
| T-02-02 | Tampering | style.css CSS merge | accept | Edit is purely subtractive (removing a duplicate block); no new selectors, no new properties from external sources; visual regression is the only risk and is covered by browser verification |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — file edits and git staging only |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "^\.navbar-nav \.nav-link {" src/style.css` — must return 1
2. `grep -c 'className="tech-icon-images"' src/components/About/Techstack.js` — must return 16
3. `grep -c '<img' src/components/About/Techstack.js` — must return 16 (counts match, all have class)
4. `git diff --cached --name-only` — must list all 6 SVG files
5. `npm start` — dev server starts without errors
6. Browser: About page > Tech Stack section — all icons render at same height as Toolstack icons
7. Browser: Navbar links render correctly (not broken by CSS merge)
</verification>

<success_criteria>
- style.css has exactly one standalone .navbar-nav .nav-link block containing: color, display, justify-content, align-items, padding, padding-right, padding-left, line-height
- All 16 img tags in Techstack.js have className="tech-icon-images"
- All 6 SVG files (numpy, graph, Matlab, typescript, n8n, sentry) are staged/committed
- Tech Stack icons on About page match Toolstack icon height (1.6rem)
- Navbar visual appearance is unchanged
</success_criteria>

<output>
Create `.planning/phases/01-code-style-fixes/01-02-SUMMARY.md` when done.
</output>
