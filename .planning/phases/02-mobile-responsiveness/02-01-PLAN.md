---
phase: "02-mobile-responsiveness"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/style.css
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02
  - MOBL-03
must_haves:
  truths:
    - "src/style.css contains no `@media (max-width: 768px)` in a max-width context — both occurrences are changed to 767px"
    - "src/style.css contains a base-level `.about-img { padding-top: 120px; }` rule (outside any media query)"
    - "src/style.css contains `.nbrr-collage` with four-column grid at base and two-column grid at max-width: 767px"
  artifacts:
    - path: "src/style.css"
      provides: "Breakpoint normalization (D-06), MOBL-01 CSS side, NBRRCard collage responsive grid"
      contains: "max-width: 767px"
  key_links:
    - from: "src/components/About/About.js"
      to: "src/style.css"
      via: ".about-img className"
      pattern: "\\.about-img"
    - from: "src/components/Projects/Robotics/NBRRCard.js"
      to: "src/style.css"
      via: ".nbrr-collage className"
      pattern: "\\.nbrr-collage"
---

<objective>
Apply all CSS-side changes to src/style.css for Phase 2: fix the breakpoint inconsistency (D-06), add the .about-img base padding rule (MOBL-01), and add the .nbrr-collage responsive grid.

Purpose: Establish the correct CSS foundation before JSX edits and before the audit. The breakpoint fix must land first so all subsequent manual testing uses the corrected typography scale and timeline layout.
Output: Updated src/style.css with three change sets applied — breakpoint normalization, .about-img base rule, .nbrr-collage rule set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mobile-responsiveness/02-CONTEXT.md
@.planning/phases/02-mobile-responsiveness/02-RESEARCH.md
@.planning/phases/02-mobile-responsiveness/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix breakpoint inconsistency in style.css (D-06, per D-06)</name>
  <files>src/style.css</files>
  <read_first>
    src/style.css (read the full file — identify the two `max-width: 768px` occurrences at lines 48 and 773, and confirm the `min-width: 768px` at line 261 must NOT be changed)
  </read_first>
  <action>
    In src/style.css, change EXACTLY two `max-width: 768px` values to `max-width: 767px`:

    1. Line 48: The font-scale block opening — change `@media (max-width: 768px)` to `@media (max-width: 767px)`. This block sets CSS custom properties for font sizes on mobile. Changing it ensures the mobile font scale no longer applies at the first tablet pixel (768px).

    2. Line 773: The timeline mobile stack block opening — change `@media (max-width: 768px)` to `@media (max-width: 767px)`. This block controls `.timeline-track` and related elements stacking vertically on mobile.

    DO NOT change the `min-width: 768px` at line 261 (the `.nav-icon-label` desktop layout rule) — that is the correct tablet-up breakpoint.

    Per D-06: the canonical mobile breakpoint for this codebase is `max-width: 767px`. The value 768px is Bootstrap's first `md` tablet pixel and must not be used in mobile-only `max-width` media queries.
  </action>
  <verify>
    <automated>grep -n "max-width: 768px" src/style.css</automated>
    Expected output: zero lines matching `max-width: 768px`. Any match is a failure.
    Also run: grep -n "min-width: 768px" src/style.css
    Expected output: line 261 still present (the nav-icon-label desktop rule).
  </verify>
  <acceptance_criteria>
    - grep -c "max-width: 768px" src/style.css returns 0
    - grep -c "max-width: 767px" src/style.css returns 3 or more (the two changed lines plus the existing one at ~line 608 for .about-img)
    - grep -c "min-width: 768px" src/style.css returns 1 (the line 261 nav rule, unchanged)
  </acceptance_criteria>
  <done>Both max-width: 768px occurrences are changed to max-width: 767px. The min-width: 768px nav rule is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add .about-img base padding rule and .nbrr-collage responsive grid (MOBL-01, D-03, D-05)</name>
  <files>src/style.css</files>
  <read_first>
    src/style.css (after Task 1 edits — locate the existing `@media (max-width: 767px) { .about-img { padding-top: 0 !important; } }` block, which is around original line 608. Also find where to add the .nbrr-collage rules, following the same mobile override form used throughout the file.)
  </read_first>
  <action>
    Two additions to src/style.css:

    ADDITION 1 — .about-img base rule (MOBL-01 CSS side, per D-03 and D-05):
    Before the existing `@media (max-width: 767px) { .about-img { padding-top: 0 !important; } }` block, add a base-level rule:

      .about-img {
        padding-top: 120px;
      }

    This is added as a BASE-LEVEL rule (not inside any media query). The existing mobile override (`padding-top: 0 !important`) already exists inside the `@media (max-width: 767px)` block and must NOT be duplicated. The result is: desktop shows 120px top padding (matching the removed inline style), mobile resets to 0 (existing rule takes effect).

    Per D-05: desktop appearance is unchanged — the 120px value is preserved, just moved from JSX to CSS. Per D-03: the `.about-img` class is already applied to the Col element in About.js.

    ADDITION 2 — .nbrr-collage responsive grid:
    Add the following ruleset to src/style.css in the Projects/Robotics section (near any existing .nbrr or projects rules, or at the end of the file before any closing comment):

      .nbrr-collage {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr 1fr;
        gap: 8px;
      }

      @media (max-width: 767px) {
        .nbrr-collage {
          grid-template-columns: 1fr 1fr;
        }
      }

    This rule set owns the grid definition for the NBRRCard collage row, replacing the inline gridTemplateColumns style that will be removed in Plan 02 Task 1. At 375px, 1fr 1fr gives two larger thumbnails instead of four tiny ones. Must use max-width: 767px per D-06.
  </action>
  <verify>
    <automated>grep -n "padding-top: 120px" src/style.css</automated>
    Expected: at least one line showing `.about-img` base rule with `padding-top: 120px` outside any media query block.
    <automated>grep -n "nbrr-collage" src/style.css</automated>
    Expected: at least two lines — the base rule and the media query override.
  </verify>
  <acceptance_criteria>
    - src/style.css contains `.about-img {` followed by `padding-top: 120px;` at base level (not inside any `@media` block)
    - src/style.css still contains `@media (max-width: 767px)` block with `.about-img { padding-top: 0 !important; }` (the existing mobile reset — not duplicated, just preserved)
    - src/style.css contains `.nbrr-collage` with `grid-template-columns: 1fr 1fr 1fr 1fr` at base level
    - src/style.css contains `@media (max-width: 767px) { .nbrr-collage { grid-template-columns: 1fr 1fr; } }`
    - grep -c "padding-top: 0 !important" src/style.css returns 1 (not duplicated)
  </acceptance_criteria>
  <done>.about-img has a 120px base rule and the mobile reset intact. .nbrr-collage responsive grid is defined. No duplicated rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan touches only static CSS files with no data flow, authentication, user input, or network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | N/A | accept | CSS/JSX layout edits — no identity or authentication surface |
| T-02-02 | Tampering | N/A | accept | No data persistence or server-side logic; changes are local stylesheet edits |
| T-02-03 | Repudiation | N/A | accept | No user actions or audit trails involved |
| T-02-04 | Information Disclosure | N/A | accept | No PII, secrets, or sensitive data in CSS files |
| T-02-05 | Denial of Service | N/A | accept | Static CSS; no server resources affected |
| T-02-06 | Elevation of Privilege | N/A | accept | No access control in scope |
| T-02-SC | Tampering | npm/pip/cargo | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
1. grep -c "max-width: 768px" src/style.css returns 0
2. grep -c "max-width: 767px" src/style.css returns 3+
3. grep -c "padding-top: 120px" src/style.css returns 1 (base .about-img rule)
4. grep -c "nbrr-collage" src/style.css returns 2+ (base + media query)
5. grep -c "min-width: 768px" src/style.css returns 1 (nav rule unchanged)
</verification>

<success_criteria>
- src/style.css contains zero `max-width: 768px` occurrences
- .about-img has a base padding-top: 120px rule and the mobile reset (0 !important) is intact
- .nbrr-collage responsive grid is defined with 4-col desktop and 2-col mobile
- min-width: 768px nav rule at line 261 is unchanged
</success_criteria>

<output>
Create `.planning/phases/02-mobile-responsiveness/02-01-SUMMARY.md` when done
</output>
