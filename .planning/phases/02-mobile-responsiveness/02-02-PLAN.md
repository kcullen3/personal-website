---
phase: "02-mobile-responsiveness"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/About/About.js
  - src/components/Projects/Robotics/NBRRCard.js
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02
must_haves:
  truths:
    - "About.js Col with className='about-img' has no paddingTop in its inline style"
    - "NBRRCard.js collage div uses className='nbrr-collage' instead of inline gridTemplateColumns"
  artifacts:
    - path: "src/components/About/About.js"
      provides: "MOBL-01 JSX side — inline paddingTop removed, paddingBottom retained"
      contains: "about-img"
    - path: "src/components/Projects/Robotics/NBRRCard.js"
      provides: "NBRRCard collage uses CSS class for responsive grid"
      contains: "nbrr-collage"
  key_links:
    - from: "src/components/About/About.js"
      to: "src/style.css"
      via: ".about-img className"
      pattern: "className=\"about-img\""
    - from: "src/components/Projects/Robotics/NBRRCard.js"
      to: "src/style.css"
      via: ".nbrr-collage className"
      pattern: "className=\"nbrr-collage\""
---

<objective>
Remove inline responsive styles from two JSX component files: the paddingTop from About.js (MOBL-01) and the inline gridTemplateColumns from NBRRCard.js. Both elements receive their layout from CSS classes defined in Plan 01.

Purpose: Complete the inline-to-CSS-class migration. These JSX changes pair with the style.css additions in Plan 01 (Wave 1 parallel sibling). Neither JSX file depends on Plan 01 completing — the className additions are valid JSX regardless of when style.css is edited.
Output: About.js with paddingBottom-only inline style on about-img Col. NBRRCard.js with nbrr-collage className replacing inline grid style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mobile-responsiveness/02-CONTEXT.md
@.planning/phases/02-mobile-responsiveness/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove paddingTop inline style from About.js about-img Col (MOBL-01, per D-03, D-04, D-05)</name>
  <files>src/components/About/About.js</files>
  <read_first>
    src/components/About/About.js (read the full file — confirm the Col at approximately lines 52-62 has `md={5}`, `className="about-img"`, and `style={{ paddingTop: "120px", paddingBottom: "50px" }}`)
  </read_first>
  <action>
    In src/components/About/About.js, locate the Col element with `md={5}` and `className="about-img"` (approximately lines 52-62 based on research).

    Change the inline `style` prop from:
      style={{ paddingTop: "120px", paddingBottom: "50px" }}

    To:
      style={{ paddingBottom: "50px" }}

    Only `paddingTop: "120px"` is removed. `paddingBottom: "50px"` is retained as-is. The `className="about-img"` attribute stays on the element — it is the hook for the CSS rule added in Plan 01.

    Per D-03: The padding value (120px) is now owned by the `.about-img` CSS class in style.css.
    Per D-04: No additional mobile treatment needed — the column stacks correctly below Bootstrap md breakpoint.
    Per D-05: Desktop top spacing is unchanged — 120px is preserved in CSS.

    Do not touch any other element, import, or part of About.js.
  </action>
  <verify>
    <automated>grep -n "paddingTop" src/components/About/About.js</automated>
    Expected output: zero lines. Any match containing "paddingTop" is a failure.
    <automated>grep -n "paddingBottom" src/components/About/About.js</automated>
    Expected output: one line with `paddingBottom: "50px"` on the about-img Col.
    <automated>grep -n "about-img" src/components/About/About.js</automated>
    Expected output: one line with `className="about-img"` on the Col element.
  </verify>
  <acceptance_criteria>
    - grep -c "paddingTop" src/components/About/About.js returns 0
    - grep -c "paddingBottom" src/components/About/About.js returns 1 (the 50px value is retained)
    - grep -c "about-img" src/components/About/About.js returns 1 (className still present)
    - The Col element still has `md={5}` prop (not altered)
  </acceptance_criteria>
  <done>About.js Col with className="about-img" has no paddingTop in its inline style. paddingBottom: "50px" is retained. about-img className is present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace inline collage grid with nbrr-collage className in NBRRCard.js (per D-02)</name>
  <files>src/components/Projects/Robotics/NBRRCard.js</files>
  <read_first>
    src/components/Projects/Robotics/NBRRCard.js (read the full file — confirm the collage div at approximately lines 119-123 uses `style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr 1fr", gap: "8px" }}` and has no existing className)
  </read_first>
  <action>
    In src/components/Projects/Robotics/NBRRCard.js, locate the div with inline style containing `display: "grid"`, `gridTemplateColumns: "1fr 1fr 1fr 1fr"`, and `gap: "8px"` (approximately lines 119-123 based on research).

    Replace the entire `style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr 1fr", gap: "8px" }}` inline style with:
      className="nbrr-collage"

    The resulting element should be:
      <div className="nbrr-collage">

    The four child elements (images, video, etc.) inside the div require no changes. Do not add any style prop to this div — the CSS class in style.css (added in Plan 01) owns the full grid definition.

    Do not touch any other element, import, or part of NBRRCard.js. The card border/shadow wrapper div with its own inline style is a different element and must not be modified.
  </action>
  <verify>
    <automated>grep -n "gridTemplateColumns" src/components/Projects/Robotics/NBRRCard.js</automated>
    Expected output: zero lines. Any match is a failure.
    <automated>grep -n "nbrr-collage" src/components/Projects/Robotics/NBRRCard.js</automated>
    Expected output: one line with `className="nbrr-collage"`.
  </verify>
  <acceptance_criteria>
    - grep -c "gridTemplateColumns" src/components/Projects/Robotics/NBRRCard.js returns 0
    - grep -c "nbrr-collage" src/components/Projects/Robotics/NBRRCard.js returns 1
    - The card border/shadow wrapper div (border, borderRadius, boxShadow, backgroundColor, padding, marginBottom inline styles) is unchanged
  </acceptance_criteria>
  <done>NBRRCard.js collage div uses className="nbrr-collage" with no inline grid styles. Card wrapper is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | JSX layout edits only — no data flow, authentication, user input, or network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | N/A | accept | Pure JSX structural edits — no identity surface |
| T-02-02 | Tampering | N/A | accept | No data persistence; changes are component attribute edits only |
| T-02-03 | Repudiation | N/A | accept | No user actions or audit trails involved |
| T-02-04 | Information Disclosure | N/A | accept | No PII, secrets, or sensitive data in JSX layout files |
| T-02-05 | Denial of Service | N/A | accept | No server resources affected |
| T-02-06 | Elevation of Privilege | N/A | accept | No access control in scope |
| T-02-SC | Tampering | npm/pip/cargo | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
1. grep -c "paddingTop" src/components/About/About.js returns 0
2. grep -c "paddingBottom" src/components/About/About.js returns 1
3. grep -c "about-img" src/components/About/About.js returns 1
4. grep -c "gridTemplateColumns" src/components/Projects/Robotics/NBRRCard.js returns 0
5. grep -c "nbrr-collage" src/components/Projects/Robotics/NBRRCard.js returns 1
</verification>

<success_criteria>
- About.js: paddingTop removed from about-img Col inline style; paddingBottom: "50px" retained; className="about-img" present
- NBRRCard.js: inline gridTemplateColumns replaced with className="nbrr-collage"; card wrapper div unchanged
</success_criteria>

<output>
Create `.planning/phases/02-mobile-responsiveness/02-02-SUMMARY.md` when done
</output>
